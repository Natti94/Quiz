name: Release Version

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    runs-on: ubuntu-latest
    # Run everything in the repository root so semantic-release reads the
    # root package.json for the monorepo release.
    defaults:
      run:
        working-directory: .
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Ensure tags are fetched
        # make sure we have all remote tags and show the last 10 (debug helper)
        run: |
          git fetch --tags --force --prune origin
          echo "Last 10 tags:" && git tag -l | tail -n 10

      - name: Install dependencies
        run: npm ci || npm i --no-audit --no-fund

      - name: Delete existing tag if present
        run: |
          TAG=$(npx semantic-release --dry-run | grep 'Release note for version' | awk '{print $NF}')
          if [ -n "$TAG" ] && git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Deleting existing tag $TAG"
            git tag -d "$TAG"
            git push --delete origin "$TAG"
          else
            echo "No existing tag to delete."
          fi
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.QUIZ_RELEASE_TOKEN }}

      - name: Semantic Release
        run: npx semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.QUIZ_RELEASE_TOKEN }}