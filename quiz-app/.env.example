# Public env variables for Vite (safe to commit)
# Note: Only variables prefixed with VITE_ are exposed to the client.

# Optional base for Netlify Functions in non-Netlify dev (leave empty normally)
# VITE_FUNCTIONS_BASE="/.netlify/functions"

# UI unlock or feature flag used by the app
VITE_SECRET_KEY="change-me"

# External projects link consumed by nav and getAssets function
VITE_CLOUDINARY_PROJECTS_LINK="https://example.com/projects"
